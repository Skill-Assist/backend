version: "3.8"
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: backend
    ports:
      - 80:80
    env_file:
      - backend/.env
    volumes:
      - ./backend:/usr/src/app
